name: Secrets - Fail
on:
  push:
#This workflow file will be modified in a separate branch to attempt to access all repository secrets. Refer to instructions on how to trigger the detection
jobs:
  build:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Set up ubuntu-latest
      run: echo "success"
