name: Workflow Run Policy Block Simulation - compromised action

on:
  workflow_dispatch:
  pull_request:

jobs:
  workflow-policy-block-compromised-action:

  
    runs-on: ubuntu-latest
    steps:
      - name: Set up ubuntu-latest
        run: echo "success"

   #Block based on compromised action (using a dummy compromised action)  
      - name: Use a compromised action
        uses: step-security/dummy-compromised-action@main
